{
  "name": "alimyandi-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "admin-panel",
    "presentation"
  ],
  "scripts": {
    "dev": "cd unified-app && npm run dev",
    "dev:unified": "cd unified-app && npm run dev",
    "build": "cd unified-app && npm run build",
    "install:all": "npm install && cd unified-app && npm install",
    "clean": "rm -rf node_modules unified-app/node_modules unified-app/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5"
  }
}
